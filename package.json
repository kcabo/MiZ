{
  "name": "miz",
  "version": "1.0.0",
  "private": true,
  "description": "create your own certification of swimming race on the cloud!",
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "serverless offline",
    "expose": "lt --port 3000 --subdomain ${LOCALTUNNEL_SUBDOMAIN} --print-requests",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/s3-request-presigner": "^3.39.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.84",
    "localtunnel": "^2.0.2",
    "serverless": "^2.63.0",
    "serverless-esbuild": "^1.20.0",
    "serverless-offline": "^8.2.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  }
}
